<h1>Here is your friendship with <%= "#{@friendship.id}" %></h1>
<% @links.each do |link| %>
	<li><a href="<%= link.url %>" target="_blank" ><%= link.url %></a></li>
<% end %>
<%= link_to "Add a link", new_friendship_link_path(@friendship)%>